/**
 * The app's current phase tag — bumped by hand as the last step of each
 * phase's release workflow (see CLAUDE.md), same moment README/CHANGELOG
 * get updated. Not package.json's version, which isn't tracked per-phase.
 */
export const APP_VERSION = "phase06";
